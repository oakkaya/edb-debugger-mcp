[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests that require a real GDB process
    slow: marks tests that are slow
    parser: marks parser unit tests
filterwarnings =
    ignore::DeprecationWarning
